% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_mixing_trace}
\alias{plot_mixing_trace}
\title{Plot mixing-proportion MCMC traces}
\usage{
plot_mixing_trace(
  result,
  components = NULL,
  ncol = 3L,
  free_y = TRUE,
  show_running_mean = TRUE,
  show_posterior_mean = TRUE,
  show_legend = TRUE,
  trace_col = "grey70",
  runmean_col = "#D55E00",
  mean_col = "#0072B2"
)
}
\arguments{
\item{result}{A \code{mcmcgraph_result} returned by \code{\link[=run_mcmc_binary]{run_mcmc_binary()}}.}

\item{components}{Integer vector selecting mixture components. \code{NULL} plots
all components.}

\item{ncol}{Number of panel columns.}

\item{free_y}{If \code{TRUE}, use a separate y-axis range for each component. If
\code{FALSE}, use one common range across all selected components.}

\item{show_running_mean}{Draw the cumulative running mean.}

\item{show_posterior_mean}{Draw a dashed horizontal posterior-mean line.}

\item{show_legend}{Add a compact legend to the first panel.}

\item{trace_col, runmean_col, mean_col}{Colours for posterior draws, running
means and posterior means.}
}
\value{
Invisibly, a data frame containing the selected component numbers
and posterior means. Called primarily for its plotting side effect.
}
\description{
Draw one trace panel for each selected mixture component. The ordinate is
labelled explicitly as the mixing proportion, while individual panels are
identified by the corresponding mathematical symbol, \ifelse{html}{\out{
&pi;<sub>j</sub>}}{\eqn{\pi_j}}. By default, panels use separate y-axis
ranges so that convergence remains visible for both large and small
components.
}
\examples{
\donttest{
data(example_binary)
fit <- run_mcmc_binary(example_binary$y, J = 3,
                       times = example_binary$times,
                       niter = 300, seed = 1)
plot_mixing_trace(fit)
}
}
