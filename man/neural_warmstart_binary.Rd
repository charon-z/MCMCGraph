% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neural_warm_start.R
\name{neural_warmstart_binary}
\alias{neural_warmstart_binary}
\title{Build a neural-network warm start for paired Bayesian MCMC clustering}
\usage{
neural_warmstart_binary(
  train_x,
  train_z,
  target_x,
  J = NULL,
  train_format = c("auto", "wide", "long"),
  target_format = c("auto", "wide", "long"),
  times = NULL,
  hidden = 12L,
  decay = 1e-04,
  maxit = 300L,
  seed = 123,
  trace = FALSE
)
}
\arguments{
\item{train_x}{labelled training trajectories in the same format as
\code{\link[=as_binary_data]{as_binary_data()}}.}

\item{train_z}{integer training labels in \code{1:J}.}

\item{target_x}{target trajectories to initialize.}

\item{J}{number of clusters. Defaults to \code{max(train_z)}.}

\item{train_format, target_format}{data formats passed to \code{\link[=as_binary_data]{as_binary_data()}}.}

\item{times}{optional time grid for each individual.}

\item{hidden}{number of hidden units in the neural initializer.}

\item{decay}{weight decay passed to \code{nnet::nnet}.}

\item{maxit}{maximum neural-network optimization iterations.}

\item{seed}{random seed for neural-network training.}

\item{trace}{whether \code{nnet} should print training progress.}
}
\value{
An initialization list with \code{z}, \code{beta}, \code{v_sq}, \code{phi} and \code{p}, plus
neural predicted probabilities and training metadata.
}
\description{
This helper trains a small single-hidden-layer neural classifier on labelled
simulated paired trajectories, predicts cluster probabilities for target
trajectories, and converts the predicted labels into the initialization list
expected by \code{\link[=run_mcmc_binary]{run_mcmc_binary()}}. The neural network only chooses the starting
point; posterior inference is still performed by the Bayesian MCMC model.
}
